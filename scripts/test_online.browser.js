// Compatibility entry point: the former iframe/guest harness now needs independent
// account cookies. Run with Node; the replacement uses two real browser contexts.
import './test_paddock_browser.js';
